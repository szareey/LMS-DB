<!-- Initialize Slider -->
<div class="row">
  <div class="col s12 l7 offset-l1">
    <div id="slider"></div>
  </div>
</div>

<!-- Initialize audio -->
<audio id='theAudio'>
  <!-- may to need to eventually change @answers to accomodate @lesson audio -->
  <% if @answer %>
    <source src='<%=@answer.audio%>' type='audio/wav'>
  <% elsif @lesson %>
    <source src='<%=@lesson.audio%>' type='audio/wav'>
  <% end %>
  Must run on chrome
</audio>